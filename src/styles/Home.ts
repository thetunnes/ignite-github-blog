
import styled from 'styled-components';
import { GlobalContainer } from './global';

export const HomeContainer = styled(GlobalContainer)`

  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 3rem;

  margin-top: -5rem;
`